services:
  backend:
    image: google-calendar-backend
    container_name: backend
    build:
      context: ./backend
      dockerfile: Dockerfile
      secrets:
        - credentials
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - FLASK_APP=app.py
    volumes:
      - ./backend/creds:/app/creds

  cli:
    image: google-calendar-backend
    container_name: cli
    build:
      context: ./backend
      dockerfile: Dockerfile.cli
      secrets:
        - credentials
    networks:
      - default
    volumes:
      - ./backend/creds:/app/creds # Mount the credentials directory
    depends_on:
      - backend

  frontend:
    image: google-calendar-frontend
    container_name: frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=production
      - VITE_BACKEND_URL=http://localhost:8080/api
    depends_on:
      - backend

  nginx:
    image: nginx:alpine
    container_name: nginx
    ports:
      - 8080:80
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend
      - frontend

secrets:
  credentials:
    file: ./backend/creds/credentials.json

volumes:
  creds:
